spring:
  application:
    name: user-service  
  jpa:
    hibernate:
      ddl-auto: validate
    open-in-view: false
  liquibase:
    change-log: classpath:db/db.changelog.xml
  datasource:
    url: jdbc:postgresql://develophost:5432/microservices_template_user_service_db
    username: postgres
    password: root
  swagger:
    autostart: false
  mvc:
    log-resolved-exception: false
  sleuth:
    sampler:
      probability: 1.0
  zipkin:
    base-url: http://develophost:9411/
    collector:
      kafka: 
        bootstrap-servers: localhost:9092
    service:
      name: user-service
    sender:
      type: kafka
    kafka:
      topic: zipkin  
  kafka:
    bootstrap-servers: develophost:9092
    client-id: user-service
    consumer:
      auto-offset-reset: earliest
      properties:
        allow.auto.create.topics: true 
    producer:
      auto-offset-reset: earliest
      properties:
        allow.auto.create.topics: true  

springdoc:
  api-docs:
    path: /api-docs    

eureka:
  instance:
    hostname: develophost
  client:
    region: default
    register-with-eureka: true
    fetch-registry: true
    registryFetchIntervalSeconds: 20
    serviceUrl:
      defaultZone: http://admin:admin@develophost:8082/template/eureka/    

server:
  port: 8084
  forward-headers-strategy: framework
  servlet:
    context-path: /template
