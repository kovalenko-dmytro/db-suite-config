spring:
  application:
    name: discovery-service
  security:
    user:
      name: admin
      password: admin
      roles: SYSTEM    

eureka:
  instance:
    hostname: develophost
  client:
    register-with-eureka: false
    fetch-registry: false
    serviceUrl:
      defaultZone: http://admin:admin@develophost:8082/eureka/    

server:
  port: 8082
  servlet:
    context-path: /template
